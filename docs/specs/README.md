# Specs

This directory is the canonical home for feature specifications and implementation plans.

Use one spec file per feature when possible. If a task list becomes large or carries useful historical progress, keep it beside the spec as `*-implementation-plan.md` in this same directory.

Do not create a separate `docs/plans/` folder. Keeping requirements, acceptance criteria, and execution plans together avoids split source-of-truth drift.
